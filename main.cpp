#include <iostream>

#include "Board_state.h"

int main()
{
	std::array<std::array, 2>, 2 a = {{1,2}, {3,4}};
	std::reverse(array.begin(), array.end());
	for(const auto & ar : a)
	{
		std::cout << ar[0] << ar[1] << "\n";
	}
}
